package com.example.ctor;

import com.example.*;
import com.example.external.*;
import com.asakusafw.vocabulary.flow.*;

@FlowPart
public class ViolateExternOutputNotProjective<T extends CharSequence> extends FlowDescription {

    public ViolateExternOutputNotProjective(
            In<String> in,
            Out<String> out,
            @Export(name = "out", description = StringExporter.class) Out<T> exporter) {
    }

    @Override
    protected void describe() {}
}
