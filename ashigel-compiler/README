Asakusa DSLのコンパイラ。

主に以下のパッケージから構成される。

* com.asakusafw.compiler.operator
Operator DSLのコンパイラエンジン。

* com.asakusafw.compiler.flow
Flow DSLのコンパイラエンジン。

* com.asakusafw.compiler.batch
Batch DSLのコンパイラエンジン。

* com.asakusafw.compiler.repository
拡張ポイントを利用した各種インターフェースの実装。

* com.asakusafw.compiler.testing
テストドライバー向けのコンパイラAPI。
