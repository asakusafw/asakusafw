<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <name>Asakusa Operator DSL Compiler Assembly</name>
  <artifactId>asakusa-mapreduce-compiler-operator</artifactId>
  <parent>
    <artifactId>project</artifactId>
    <groupId>com.asakusafw.mapreduce.compiler</groupId>
    <version>0.9.2-SNAPSHOT</version>
  </parent>

  <packaging>jar</packaging>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <artifactSet>
                <includes>
                  <include>com.asakusafw</include>
                  <include>${project.groupId}</include>
                  <include>org.slf4j</include>
                </includes>
              </artifactSet>
              <filters>
                <filter>
                  <artifact>${project.groupId}:asakusa-mapreduce-compiler-core</artifact>
                  <includes>
                    <include>META-INF/services/com.asakusafw.compiler.operator.*</include>
                    <include>com/asakusafw/compiler/common/**</include>
                    <include>com/asakusafw/compiler/operator/**</include>
                    <include>com/asakusafw/compiler/repository/**</include>
                  </includes>
                </filter>
                <filter>
                  <artifact>com.asakusafw:asakusa-runtime</artifact>
                  <includes>
                    <include>com/asakusafw/runtime/model/**</include>
                    <include>com/asakusafw/runtime/value/**</include>
                    <include>com/asakusafw/runtime/core/**</include>
                  </includes>
                </filter>
              </filters>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
              </transformers>
              <shadedArtifactAttached>true</shadedArtifactAttached>
              <shadedClassifierName>lib</shadedClassifierName>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>asakusa-mapreduce-compiler-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.asakusafw</groupId>
      <artifactId>asakusa-runtime</artifactId>
      <version>${asakusafw.version}</version>
    </dependency>
    <dependency>
      <groupId>com.asakusafw</groupId>
      <artifactId>asakusa-dsl-vocabulary</artifactId>
      <version>${asakusafw.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
      <optional>true</optional>
    </dependency>
  </dependencies>
</project>
