Asakusa DSLで作成したプログラムを実行する際のライブラリ。

主に以下のパッケージから構成される。

* com.asakusafw.runtime.core
アプリケーションから直接利用するクラスやインターフェース群。

* com.asakusafw.runtime.testing
アプリケーションのテスト時に利用するクラスやインターフェース群。

* com.asakusafw.runtime.value
データモデルのプリミティブ型を表すライブラリ。

* com.asakusafw.runtime.io
データモデルの入出力に関するライブラリ。

* com.asakusafw.runtime.stage
Map Reduceジョブを実行する際に利用するライブラリ。

* com.asakusafw.runtime.flow
データフローの表現に利用するMap Reduce用のライブラリ。

* com.asakusafw.runtime.report
レポートAPIインターフェースの実装クラス群。

==
Report API via Commons Logging.

<configuration>
    <property>
        <name>com.asakusafw.runtime.core.Report.Delegate</name>
        <value>com.asakusafw.runtime.report.CommonsLoggingReport</value>
    </property>
</configuration>

==


